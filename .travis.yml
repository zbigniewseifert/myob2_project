language: python
services:
  - docker

before_install: 
  - docker build -t zseifert/vote_app myob2/.
  - docker run -d -p 8888:8888 zseifert/vote_app test_application.py
  - docker ps -a

script: pytest
